name: CI Workflow

on:
  push:
    branches: [ "main", "conflict_170826_2155" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'

    - name: Run test check
      run: |
        echo "Workflow file created successfully!"
